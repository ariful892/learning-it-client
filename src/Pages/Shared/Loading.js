import React from 'react';

const Loading = () => {
    return (
        <div>
            <h2 className='text-3xl text-primary text-center'>Loading...!</h2>
        </div>
    );
};

export default Loading;