import React from 'react';
import reg from '../../assets/reg-bg.png'

const Register = () => {
    return (
        <div
            style={{ background: `url(${reg})` }}
            className="min-h-screen">

        </div>
    );
};

export default Register;